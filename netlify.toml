[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm --prefix client run dev"
  targetPort = 5173
  port = 8888
  # Connect to Render backend - replace with your actual Render URL
  env = { VITE_API_URL = "https://your-backend-app.onrender.com" }
