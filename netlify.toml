[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm --prefix client run dev"
  targetPort = 5173
  port = 8888
  # Auto-link frontend to local backend port
  env = { VITE_API_URL = "http://127.0.0.1:5000" } 