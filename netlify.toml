[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm --prefix client run dev"
  targetPort = 5173
  port = 8888
  # Connect to local backend for development
  env = { VITE_API_URL = "http://localhost:5555" }
