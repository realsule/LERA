{
  "name": "lera-monorepo",
  "version": "1.0.0",
  "description": "LERA Event Management System - Full Stack Monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && python server/app.py\" \"cd client && npm run dev\"",
    "backend": "cd backend && python server/app.py",
    "frontend": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["lera", "events", "react", "flask", "monorepo"],
  "author": "LERA Team"
}
